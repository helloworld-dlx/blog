---
import BaseLayout from '../layouts/BaseLayout.astro';
import Comments from '../components/Comments.astro';
---

<BaseLayout title="关于" description="关于 dlx">
  <div class="about-layout">
    <aside class="about-nav" aria-label="关于页面目录">
      <div class="about-mark">dlx</div>
      <a href="#who">01 · 我是谁</a>
      <a href="#learning">02 · 我现在在学什么</a>
      <a href="#building">03 · 我正在构建什么</a>
      <a href="#why">04 · 为什么写这个博客</a>
      <a href="#direction">05 · 我的长期方向</a>
      <a href="#contact">06 · 找到我</a>
    </aside>

    <article class="about">
      <section class="intro">
        <h1>About</h1>
      </section>

      <section id="who">
        <h2><span>01</span>我是谁</h2>
        <p>你好，我是 dlx。一名集成电路设计专业学生。</p>
        <p>相较于在应用层调用写好的 API 框架，我更着迷于弄清楚一行高级语言代码，是如何跨越编译器、指令集和微架构，最终转变成硅片上成千上万个逻辑门电信号跳变的。</p>
        <p>这里是我的数字实验室，记录我作为一个底层架构学徒的折腾日常。</p>
      </section>

      <section id="learning">
        <h2><span>02</span>我现在在学什么</h2>
        <p>我的核心学习主线是计算机体系结构与数字前端设计。</p>
        <p>目前，我正处于从纯软件思维（C/C++ 顺序执行）向硬件物理思维（Verilog 并发执行）的重构期。日常不仅在和底层的指针与内存打交道，也在折腾 VMware 中的 Ubuntu 环境、Makefile 编译规则以及 Verilator 硬件仿真工具链。我在学习如何像一个工程师一样，在逻辑的严密性与物理的限制（性能、功耗、面积）之间寻找平衡。</p>
        <p>读书进度追踪：<a href="/posts/2026-05-19-reading-tracker/">读书进度看板</a></p>
      </section>

      <section id="building">
        <h2><span>03</span>我正在构建什么</h2>
        <p>在这个阶段，我的"施工现场"主要有两个：</p>
        <ul>
          <li><strong>处理器与数字硬件：</strong>依托“一生一芯”、FPGA 和课程项目，持续建立从指令集、数据通路到 RTL、仿真和硬件验证的理解。</li>
          <li><strong>个人 AI 助手 Autumn：</strong>一个跨树莓派、Windows 和移动设备运行的个人 AI 助手系统，尝试把对话、工具、文件和任务组织成一个统一入口。</li>
        </ul>
      </section>

      <section id="why">
        <h2><span>04</span>我为什么写这个博客</h2>
        <p>底层硬件和架构的学习曲线极其陡峭，且充满了 Bug和"跑不通的仿真"。</p>
        <p>建立这个博客，首先是为了给自己外挂一个"记忆硬盘"，作为长期的 Debug 日志和踩坑记录；其次，我想把在这个过程中那些突然"顿悟"的瞬间记录下来。我还会在这里放一些随笔和通识基础的笔记。</p>
      </section>

      <section id="direction">
        <h2><span>05</span>我的长期方向</h2>
        <p>长期关注处理器、计算机体系结构、数字 IC 与高效计算硬件。</p>
        <p>面对现在快速迭代的各种 AI 算法和顶层框架，我深知自己目前的算力和精力无法全盘学习。因此，我选择先潜心扎根在"地下一层"。</p>
        <p>我对 AI 加速器的底层硬件实现充满了好奇。它也是我眼中体系结构、数字硬件与 AI 真正交汇的地方。我正在努力夯实自己的体系结构基础，希望未来能有机会去深入研究如何更高效地完成底层的算术运算和数据搬运。路还很长，目前先从写好每一行 Verilog 开始。</p>
      </section>

      <section id="contact" class="contact">
        <div class="contact-heading">
          <h2><span>06</span>找到我</h2>
          <span class="contact-kicker">Elsewhere on the web</span>
        </div>
        <p>如果你也在折腾处理器、数字硬件、Agent，或者只是对这些东西感兴趣，欢迎来聊：</p>

        <div class="contact-links">
          <a class="contact-link" href="https://github.com/helloworld-dlx" target="_blank" rel="noopener">
            <span class="contact-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="currentColor">
                <path d="M12 .8a11.4 11.4 0 0 0-3.6 22.2c.6.1.8-.2.8-.6v-2.2c-3.4.7-4.1-1.5-4.1-1.5-.6-1.4-1.4-1.8-1.4-1.8-1.1-.8.1-.8.1-.8 1.2.1 1.9 1.3 1.9 1.3 1.1 1.9 2.9 1.4 3.6 1.1.1-.8.4-1.4.8-1.7-2.7-.3-5.6-1.4-5.6-6.1 0-1.3.5-2.4 1.2-3.3-.1-.3-.5-1.6.1-3.3 0 0 1-.3 3.5 1.3a12 12 0 0 1 6.4 0C17.2 3.8 18.2 4 18.2 4c.6 1.7.2 3 .1 3.3.8.9 1.2 2 1.2 3.3 0 4.8-2.9 5.8-5.6 6.1.4.4.8 1.1.8 2.2v3.4c0 .4.2.7.8.6A11.4 11.4 0 0 0 12 .8Z" />
              </svg>
            </span>
            <span class="contact-copy">
              <strong>GitHub</strong>
              <span>github.com/helloworld-dlx</span>
            </span>
            <span class="contact-arrow" aria-hidden="true">↗</span>
          </a>

          <a class="contact-link" href="mailto:1531371420@qq.com">
            <span class="contact-icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                <rect x="3" y="5" width="18" height="14" rx="2" />
                <path d="m4 7 8 6 8-6" />
              </svg>
            </span>
            <span class="contact-copy">
              <strong>Email</strong>
              <span>1531371420@qq.com</span>
            </span>
            <span class="contact-arrow" aria-hidden="true">→</span>
          </a>
        </div>
      </section>

      <Comments title="关于" />
    </article>
  </div>
</BaseLayout>

<style>
  .about-layout {
    display: grid;
    grid-template-columns: 10.5rem minmax(0, 42.5rem);
    gap: 3.5rem;
    justify-content: center;
    padding: 3.5rem 0 4.5rem;
  }

  .about-nav {
    position: sticky;
    top: calc(var(--nav-height) + 2rem);
    align-self: start;
  }

  .about-mark {
    width: 3.8rem;
    height: 3.5rem;
    display: grid;
    place-items: center;
    margin-bottom: 1.2rem;
    border-radius: 0.85rem;
    background: var(--color-accent-soft);
    color: var(--color-accent-strong);
    font-family: var(--font-mono);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.02em;
  }

  .about-nav a {
    display: block;
    padding: 0.25rem 0;
    color: var(--color-text-faint);
    font-family: var(--font-mono);
    font-size: 0.69rem;
    text-decoration: none;
  }

  .about-nav a:hover { color: var(--color-accent-strong); }

  .intro { margin-bottom: 2.2rem; }
  .intro h1 { margin: 0; font-size: 2rem; letter-spacing: -0.03em; }

  .about section { margin-bottom: 2.1rem; }

  .about h2 {
    display: flex;
    align-items: baseline;
    gap: 0.6rem;
    margin-bottom: 0.75rem;
    padding: 0;
    border: 0;
    background: transparent;
    font-size: 1.05rem;
  }

  .about h2 span {
    color: var(--color-accent-strong);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
  }

  .about p { margin-bottom: 0.65rem; color: var(--color-text-muted); }
  .about ul { margin-bottom: 0.75rem; color: var(--color-text-muted); }
  .about li { margin-bottom: 0.55rem; }

  .contact {
    margin-top: 2.35rem;
    padding-top: 0.15rem;
  }

  .contact-heading {
    display: flex;
    align-items: baseline;
    gap: 0.85rem;
    margin-bottom: 0.48rem;
  }

  .contact-heading h2 {
    margin: 0;
  }

  .contact-kicker {
    color: var(--color-text-faint);
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.02em;
  }

  .contact > p {
    margin-bottom: 0.9rem;
  }

  .contact-links {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0.7rem;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    min-width: 0;
    padding: 0.7rem 0.78rem;
    border: 1px solid var(--color-border);
    border-radius: 0.7rem;
    background: var(--color-surface);
    color: inherit;
    text-decoration: none;
    box-shadow: var(--shadow-soft);
    transition: border-color 0.15s, background 0.15s, transform 0.15s;
  }

  .contact-link:hover {
    border-color: var(--color-accent);
    background: color-mix(in srgb, var(--color-accent-soft) 58%, var(--color-surface));
    color: inherit;
    text-decoration: none;
    transform: translateY(-1px);
  }

  .contact-icon {
    display: grid;
    place-items: center;
    flex: 0 0 auto;
    width: 2.1rem;
    height: 2.1rem;
    border-radius: 0.58rem;
    background: var(--color-accent-soft);
    color: var(--color-accent-strong);
  }

  .contact-icon svg {
    width: 1.05rem;
    height: 1.05rem;
  }

  .contact-copy {
    min-width: 0;
  }

  .contact-copy strong,
  .contact-copy span {
    display: block;
  }

  .contact-copy strong {
    margin-bottom: 0.05rem;
    color: var(--color-text);
    font-size: 0.77rem;
  }

  .contact-copy span {
    overflow: hidden;
    color: var(--color-text-muted);
    font-size: 0.66rem;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .contact-arrow {
    margin-left: auto;
    color: var(--color-text-faint);
    font-size: 0.72rem;
  }

  @media (max-width: 820px) {
    .about-layout { grid-template-columns: 1fr; gap: 0; }
    .about-nav { display: none; }
  }

  @media (max-width: 560px) {
    .contact-heading {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.1rem;
    }

    .contact-links {
      grid-template-columns: 1fr;
    }
  }
</style>
